var game,y,x

game = "perereca" + "games";

x = 1;

y = 2

function plus1(x) {  //define uma função chamada plus1 com o parâmetro "x"
return x+1; //retorna uma unidade maior do que foi passado

} // as funçõs são incluídas entre chaves XD

plus1(y) // => 4: Y é 3; portanto, essa chamada retorna 3+1

var square = function(x) { //As funções são valores e podem ser atribuídas a variáveis!!
return x*x; // calcula o valor da função

}

var a = []; // cria um array vazio
a.push(1,2,3);  //o método push() adiciona elementos em-
               //-um array(lista indexada numericamente)

a.reverse(); //outro método:inverte a ordem dos elementos do array 
